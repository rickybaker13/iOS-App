import SwiftUI

extension Color {
    static let mateBackground = Color(red: 0.95, green: 0.95, blue: 0.97) // Light grayish blue
    static let matePrimary = Color(red: 0.4, green: 0.5, blue: 0.6) // Muted blue
    static let mateSecondary = Color(red: 0.6, green: 0.6, blue: 0.65) // Silver gray
    static let mateAccent = Color(red: 0.2, green: 0.3, blue: 0.4) // Darker blue
    static let mateText = Color(red: 0.2, green: 0.2, blue: 0.25) // Dark gray
    static let mateBorder = Color(red: 0.8, green: 0.8, blue: 0.85) // Light gray border
} 